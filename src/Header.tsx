import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="bg-blue-600 text-white p-4 text-center">
            <h1 className="text-5xl font-bold">Recipe App</h1>
        </header>
    );
};

export default Header;
